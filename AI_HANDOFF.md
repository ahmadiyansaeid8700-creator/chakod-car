# AI Handoff — Chakod

این فایل نقطه شروع سریع برای ادامه پروژه در هر چت جدید است و باید بعد از هر Patch به‌روزرسانی شود.

## مخزن و شاخه کاری

```text
Repository: ahmadiyansaeid8700-creator/chakod-car
Working branch: backup-latest-2026-08-03
Current published baseline: 747485c
Baseline title: standardize account listing routes
Recovery mode started: 2026-08-05
```

> شاخه `main` مبنای ادامه کار نیست. تا زمان تأیید صریح مالک پروژه، فقط روی شاخه بالا کار کن و آن را با `main` ادغام نکن.

## وضعیت قطعی GitHub

- آخرین نسخه‌ای که واقعاً روی GitHub ذخیره شده Commit `747485c` است.
- ساختار ثبت و مدیریت آگهی‌ها روی `/account/listings/*` در همین نسخه منتشر شده است.
- تعدادی Patch بعدی در یک محیط موقت ساخته شده بودند اما Push نشده‌اند و در GitHub وجود ندارند.
- هش‌های محلی گزارش‌شده مانند `fba9f31`، `b3aaf08` و `487c22e` نباید به‌عنوان نسخه قابل بازیابی فرض شوند.
- ادامه پروژه از نسخه منتشرشده GitHub انجام می‌شود؛ پروژه از صفر ساخته نمی‌شود، ولی Patchهای منتشرنشده از نو و به‌ترتیب بازسازی می‌شوند.

## حالت بازیابی

هدف فعلی بازسازی ایمن Patchهای منتشرنشده است. ترتیب شروع:

1. ممیزی وضعیت واقعی Commit `747485c` و ثبت اختلاف آن با نقشه مادر.
2. حذف فایل‌های بکاپ یا کد بلااستفاده فقط پس از تأیید مسیر و تست.
3. ساخت صفحه استاندارد فارسی و RTL برای خطای 404 در صورت نبود یا ناقص‌بودن آن.
4. ادامه یکسان‌سازی مسیرهای محصول طبق `docs/MASTER-SITEMAP-FA.md`.
5. بازسازی مرحله‌های دسترسی و مجوزها فقط بعد از رسیدن دوباره به همان فاز.

هیچ Patch بعدی نباید صرفاً براساس پیام‌های چت قبلی فرض شود؛ کد فعلی و اسناد مخزن باید قبل از هر تغییر بررسی شوند.

## قرارداد انتشار اجباری

برای جلوگیری از تکرار از دست رفتن کار:

- هر Patch باید کوچک، مستقل و قابل بازگشت باشد.
- قبل از تغییر، هدف، مسیرها و فایل‌های تحت‌تأثیر مشخص شوند.
- تست مرتبط و Build قابل اجرا پیش از انتشار انجام شود.
- کد و مستندات همان Patch در یک Commit یا Commitهای صریح و مرتبط ثبت شوند.
- Commit باید فوراً روی `backup-latest-2026-08-03` در GitHub منتشر شود.
- شروع Patch بعدی قبل از تأیید وجود Commit قبلی روی GitHub ممنوع است.
- بعد از هر انتشار، هش Commit منتشرشده در همین فایل، `PROJECT_CONTEXT.md` و `TODO.md` ثبت شود.
- فایل‌های محیطی، رمزها، Tokenها، API Keyها و Secrets هرگز Commit نشوند.

## ترتیب مطالعه در هر چت جدید

1. `AI_HANDOFF.md`
2. `docs/MASTER-SITEMAP-FA.md`
3. `docs/PROJECT-CHECKLIST-FA.md`
4. `PROJECT_CONTEXT.md`
5. `TODO.md`
6. `AGENTS.md`
7. `CLAUDE.md`
8. `package.json`

## گزارش Patchهای قطعی منتشرشده

| Commit منتشرشده | تغییر | وضعیت |
|---|---|---|
| `0e177d4` | اصلاح امن `returnTo` و محافظت مسیرهای مدیریتی | منتشرشده |
| `b5fb874` | تثبیت مسیرهای عمومی خودرو روی `/cars/*` | منتشرشده |
| `747485c` | یکسان‌سازی ثبت و مدیریت آگهی‌ها روی `/account/listings/*` | مبنای بازیابی |

## قدم بعدی

```text
Recovery Patch R1 — ممیزی Commit 747485c و بازسازی اولین Patch منتشرنشده
```

در R1 ابتدا فایل‌های بکاپ، صفحه 404، مسیرهای تکراری و وضعیت تست‌ها بررسی می‌شوند. سپس فقط اولین تغییر کوچک و قابل اثبات اجرا، تست، Commit و فوراً روی GitHub منتشر می‌شود.

## دستور آماده برای چت جدید

```text
پروژه Chakod را از مخزن ahmadiyansaeid8700-creator/chakod-car و شاخه
backup-latest-2026-08-03 ادامه بده. مبنای قطعی بازیابی Commit 747485c است.
ابتدا AI_HANDOFF.md و اسناد مرجع را بخوان. Patchهای Push‌نشده چت‌های قبلی را
موجود فرض نکن. هر Patch را کوچک اجرا کن، تست کن، Commit کن و قبل از شروع مرحله
بعد، وجود Commit را روی GitHub تأیید کن. main را تغییر نده و هیچ Secretی Commit نکن.
```

## زمان آخرین به‌روزرسانی

```text
2026-08-05 — آغاز بازیابی از آخرین نسخه قطعی GitHub
```
