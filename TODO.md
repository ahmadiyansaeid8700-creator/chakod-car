# TODO — Chakod

## وضعیت جاری

```text
Project: chakod-car
Working branch: backup-latest-2026-08-03
Latest known remote commit: e067eb8
Current product phase: 2 — پچ‌های پس از ممیزی
```

## کار عملی فوری

- [ ] نتیجه Build جدید Cloudflare برای Commit `2491a56` بررسی شود.
- [ ] در صورت شکست Build، فقط خطای همان Build ثبت و در یک پچ مستقل اصلاح شود.
- [ ] پس از موفقیت Build، نقطه بازگشت سالم ثبت شود.
- [ ] تا زمان تأیید Build، شاخه کاری با `main` ادغام نشود.

---

## مرحله ۲ — ممیزی کامل بدون تغییر کد

### ۲.۱. موجودی صفحات و مسیرها

- [x] تمام مسیرهای App Router از پوشه `app` استخراج شوند.
- [x] تمام Route Handlerها و APIها استخراج شوند.
- [x] مسیرهای عمومی، خصوصی و مدیریتی تفکیک شوند.
- [x] مسیرهای موجود با `docs/MASTER-SITEMAP-FA.md` مقایسه شوند.
- [x] مسیرهای خارج از نقشه مادر گزارش شوند.
- [x] مسیرهای نقشه مادر که هنوز وجود ندارند گزارش شوند.

### ۲.۲. ممیزی لینک‌ها و دکمه‌ها

- [x] تمام `Link`ها استخراج شوند.
- [x] تمام `href`ها استخراج شوند.
- [x] تمام `router.push`، `router.replace` و Redirectها استخراج شوند.
- [x] دکمه‌های بدون `onClick` یا عملکرد مؤثر گزارش شوند.
- [x] لینک‌های اشاره‌کننده به مسیر ناموجود گزارش شوند.
- [x] لینک‌های دارای مقصد موقت، `#` یا متن آزمایشی گزارش شوند.
- [x] مسیر بازگشت هر صفحه خصوصی بررسی شود.

### ۲.۳. صفحات ناقص و بدون ورودی

- [x] صفحات بدون لینک ورودی شناسایی شوند.
- [x] صفحات آزمایشی یا موقت شناسایی شوند.
- [x] فایل‌های بکاپ داخل مسیرهای اجرایی شناسایی شوند.
- [x] صفحات تکراری با کارکرد مشابه شناسایی شوند.
- [x] صفحات دارای محتوای ساختگی یا Placeholder گزارش شوند.

### ۲.۴. مسیرهای تکراری و تصمیم‌های ساختاری

- [x] مسیرهای نمایشگاه با ساختار نهایی مقایسه شوند.
- [x] مسیرهای کسب‌وکار با `/businesses/[slug]` مقایسه شوند.
- [x] مسیرهای همکاری در فروش و سفیر بررسی و موارد موازی گزارش شوند.
- [x] مسیرهای حساب تجاری با ساختار `/account/business/*` مقایسه شوند.
- [x] مسیرهای مدیریت قابل مشاهده یا قابل دسترسی برای کاربر عادی گزارش شوند.

### ۲.۵. خروجی ممیزی

گزارش نهایی باید دقیقاً این بخش‌ها را داشته باشد:

- [x] صفحات موجود
- [x] صفحات ناقص
- [x] دکمه‌های خراب
- [x] دکمه‌های بدون عملکرد
- [x] مسیرهای تکراری
- [x] صفحات بدون لینک
- [x] مسیرهای مدیریت قابل مشاهده برای کاربر عادی
- [x] پیشنهاد مسیر نهایی هر بخش
- [x] فهرست پچ‌های پیشنهادی با اولویت و ریسک
- [x] اعلام صریح اینکه در مرحله ممیزی هیچ کدی تغییر نکرده است

---

## پچ‌های بعد از تأیید ممیزی

> این بخش فقط پس از تأیید گزارش مرحله ۲ فعال می‌شود.

- [ ] پچ ۲.۱: لینک‌های خراب
- [ ] پچ ۲.۲: دکمه‌های بدون عملکرد
- [x] پچ ۲.۳: Redirectها و مسیر بازگشت — اصلاح امن `returnTo` ورود
- [ ] پچ ۲.۴: صفحه استاندارد 404
- [ ] پچ ۲.۵: حذف یا انتقال صفحات آزمایشی
- [ ] پچ ۲.۶: یکسان‌سازی مسیر نمایشگاه‌ها
- [ ] پچ ۲.۷: یکسان‌سازی مسیر کسب‌وکارها
- [ ] پچ ۲.۸: یکسان‌سازی مسیر همکاری در فروش
- [x] پچ ۲.۹: پنهان‌سازی کامل مسیرهای مدیریت برای کاربر عادی

هر پچ باید جداگانه Commit و تست شود.

### Patch 2.3 — اصلاح مسیر بازگشت ورود

- Phase: 2 — پچ‌های پس از ممیزی
- Purpose: بازگرداندن کاربر به مقصد ذخیره‌شده پس از ورود
- Affected routes: `/login?returnTo=...`
- Affected files: `app/login/page.tsx`، `app/login/return-to.ts`، `tests/login-return-to.test.mjs`
- Database impact: ندارد
- Environment impact: ندارد
- Rollback commit: `e067eb8`
- Tests executed: چهار تست واحد مسیر امن؛ ESLint فایل‌های پچ
- Result: موفق؛ صفر خطای ESLint و یک هشدار قدیمی مربوط به `<img>`
- Build note: Build کامل به‌علت نبود `.openai/hosting.json` و `build/sites-vite-plugin` در محیط فعلی شروع نشد.
- Follow-up: بررسی و پنهان‌سازی مسیرهای مدیریت برای کاربر عادی

### Patch 2.9 — پنهان‌سازی مسیرهای مدیریت برای کاربر عادی

- Phase: 2 — پچ‌های پس از ممیزی
- Purpose: جلوگیری از بارگذاری یا نمایش صفحات مدیریت برای کاربر غیرمدیر و حذف ورودی عمومی `/dealers`
- Affected routes: `/admin/*`، `/dealers/*`، `/showrooms`
- Affected files: `app/admin/layout.tsx`، `app/dealers/layout.tsx`، `app/admin/page.tsx`، `app/showrooms/page.tsx`، `lib/route-access.ts`، `lib/server-route-access.ts`، `tests/route-access.test.mjs`
- Database impact: ندارد
- Environment impact: ندارد
- Rollback commit: `ff8c224`
- Tests executed: هفت تست واحد ورود و دسترسی؛ ESLint فایل‌های جدید؛ TypeScript سراسری؛ Build محلی پنج‌مرحله‌ای Vinext؛ مجموعه تست‌های مستقل پروژه
- Result: Build محلی و ۱۵ تست مستقل موفق؛ ESLint فایل‌های جدید بدون خطا؛ TypeScript فقط خطاهای قدیمی Affiliate/Cloudflare را گزارش کرد. Build مخصوص Cloudflare پیش از Compile به‌علت نبود `.openai/hosting.json` و `build/sites-vite-plugin` متوقف شد و سه تست رندر وابسته به خروجی Cloudflare روی Build محلی قابل اجرا نبودند.
- Follow-up: تعیین و تثبیت مسیر نهایی خودروها

---

## قالب ثبت نتیجه هر پچ

```md
### Patch X.Y — عنوان

- Phase:
- Purpose:
- Affected routes:
- Affected files:
- Database impact:
- Environment impact:
- Rollback commit:
- Tests executed:
- Result:
- Follow-up:
```

---

## کارهای ممنوع در مرحله فعلی

- [ ] بازسازی پروژه از صفر
- [ ] ایجاد مسیر موازی جدید
- [ ] Merge با `main` پیش از تأیید Build و گزارش
- [ ] تغییر هم‌زمان فرانت‌اند، بک‌اند و دیتابیس بدون ضرورت
- [ ] اجرای `npm audit fix --force` بدون بررسی اثرات
- [ ] Commit فایل‌های محیطی یا Secrets
- [ ] حذف انبوه فایل‌ها بدون گزارش و نقطه بازگشت
