# چک‌پوینت آماده‌سازی قالب Staging — ۲۸ مرداد ۱۴۰۵

## محدوده

```text
Repository: ahmadiyansaeid8700-creator/chakod-car
Branch: agent/launch-3-local-baseline
Environment: https://staging.chakod.com
Goal: تکمیل و تست قالب و جریان‌های اصلی روی GitHub + Cloudflare Staging پیش از انتقال نهایی به زیرساخت مالک
```

## تصمیم اجرایی مالک

- [x] توسعه و تست فعلاً روی GitHub + Cloudflare Staging ادامه دارد.
- [x] انتقال سایت به زیرساخت نهایی فقط بعد از تکمیل سایت و تأیید نهایی مالک انجام می‌شود.
- [x] تا قبل از آن، انتقال زودهنگام فرانت به cPanel/هاست انجام نمی‌شود.

## مواردی که در این جلسه با تست تصویری تأیید شدند

- [x] صفحه حساب کاربر `/account` رندر می‌شود.
- [x] صفحه «آگهی‌های من» `/account/listings` رندر می‌شود و کارت‌های چند وضعیت را نمایش می‌دهد.
- [x] چیدمان کارت خودرو در عرض موبایل اصلاح شد و عنوان/برند/مدل وارد باکس مشخصات نمی‌شود.
- [x] تایپوگرافی عنوان خودرو، نام منتشرکننده و سال/کارکرد در کارت مشترک خواناتر شد.
- [x] آپلود تصویر از Staging به بک‌اند واقعی موفق است.
- [x] فایل آپلودشده روی `api.chakod.com/uploads/...` قابل دسترسی است.
- [x] URLهای قدیمی `chakod.com/uploads/...` برای نمایش در Staging به دامنه صحیح API نرمال شدند.
- [x] تصویر اصلی در صفحه ویرایش آگهی بعد از اصلاح URL نمایش داده شد.

## موارد پیاده‌سازی‌شده که باید در Smoke Test بعدی دوباره دیده شوند

- [~] صفحه مدیریت تصاویر: خطای ارتباطی باید روی همان کارت عکس با دکمه «بارگذاری مجدد» نمایش داده شود.
- [~] صفحه مدیریت تصاویر: خطای سرویس/فایل باید علت را روی همان کارت نمایش دهد و Retry کور ارائه نکند.
- [~] دکمه پایین صفحه تصاویر فقط «تأیید» است و باید کاربر را به صفحه قبل برگرداند.
- [~] وضعیت‌های فنی آگهی در صفحه ویرایش باید فارسی نمایش داده شوند و `approved` خام دیده نشود.
- [~] خطای 503 ذخیره ویرایش باید با متن کاربرپسند نمایش داده شود و عبارت فنی «بک‌اند فعال نشده» به کاربر نشان داده نشود.

## مانع واقعی باقی‌مانده — ذخیره ویرایش آگهی

- [!] Route فرانت `/api/auth/listings/edit/[id]` برای ذخیره واقعی، درخواست را به `https://api.chakod.com/api/update-listing.php` می‌فرستد.
- [!] Endpoint `update-listing.php` روی بک‌اند فعلی وجود ندارد و پاسخ 404 باعث 503 در Staging می‌شود.
- [!] سورس `listing-manage.php` و Schema دیتابیس اصلی آگهی‌ها داخل Branch فعلی GitHub نیست؛ بنابراین ساخت Endpoint با حدس نام جدول/مالک ممنوع است.
- [!] برای فعال‌سازی امن ذخیره، باید سورس یا دسترسی سرور API فراهم باشد تا Endpoint با Schema و کنترل مالکیت واقعی همان بک‌اند ساخته و Deploy شود.

## وضعیت معماری

- Staging فرانت: Cloudflare Worker.
- Repository: GitHub.
- API و فایل‌های تصاویر آگهی: `api.chakod.com`.
- D1 پروژه مخزن اصلی آگهی‌های PHP نیست و نباید برای شبیه‌سازی ویرایش واقعی یک منبع داده موازی ساخته شود.

## معیار آماده بودن قالب برای تأیید نهایی

1. صفحات حساب، آگهی‌های من، ویرایش و تصاویر روی دسکتاپ و موبایل بدون شکست Layout دیده شوند.
2. آپلود، نمایش، انتخاب عکس اصلی، حذف و Retry تصویر تست شوند.
3. تمام وضعیت‌ها و پیام‌های کاربر نهایی فارسی و غیرتکنیکی باشند.
4. ذخیره واقعی ویرایش آگهی روی بک‌اند فعال و با Refresh تأیید شود.
5. ایجاد آگهی، مدیریت وضعیت و نمایش عمومی حداقل یک بار End-to-End تست شوند.
6. بعد از تأیید مالک، نسخه پایدار برای انتقال نهایی مشخص شود.
